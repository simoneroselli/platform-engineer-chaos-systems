appFullname: votepoll-test-app
version: 0.1.0
app:
  frontend:
    replicaCount: 1
    container:
      tag: before
      port: 80
  redis:
    resources:
      limits:
        cpu: "200m"
        memory: "256Mi"
      requests:
        cpu: "100m"
        memory: "128Mi"
 
svc:
  frontend:
    port: 80
    targetPort: 80
    nodePort: 30001
  redis:
    port: 6379
    targetPort: 6379